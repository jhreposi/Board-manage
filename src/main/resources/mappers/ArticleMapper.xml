<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board.repository.ArticleMapper">

    <select id="selectNoticeList" resultType="Article">
        SELECT
            a.article_id,
            a.title,
            a.content,
            a.created_at,
            a.updated_at,
            c.name AS categoryName,
            b.name AS boardName,
            m.username
        FROM article a
            INNER JOIN board b      ON a.board_id = b.board_id
            INNER JOIN category c   ON b.board_id = c.board_id
            INNER JOIN member m     ON a.member_id = m.member_id

    </select>
</mapper>