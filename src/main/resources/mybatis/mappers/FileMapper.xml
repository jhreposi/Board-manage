<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board.repository.FileMapper">

    <insert id="insertFile" parameterType="FileVo">
        INSERT INTO
            file
            (filename, original_name, file_path, file_size, extenstion, article_id)
        VALUES
            (#{filename}, #{originalName}, #{filePath}, #{fileSize}, #{extension}, #{articleId})
    </insert>

    <select id="selectFilesByArticleId" parameterType="int" resultType="FileVo">
        SELECT
            file_id,
            original_name,
            article_id
        FROM
            file
        WHERE
            article_id = #{articleId}
    </select>

    <select id="selectFileById" resultType="FileVo" parameterType="int">
        SELECT
            file_id,
            article_id,
            filename,
            original_name,
            file_path
        FROM
            file
        WHERE
            file_id = #{fileId}
    </select>

</mapper>